---
dns:
  hosts:
    thefutureisprivate-dev:
      ansible_connection: 'local'
      ansible_host: 'localhost'
    thefutureisprivate-app:
      ansible_connection: 'local'
      ansible_host: 'localhost'
