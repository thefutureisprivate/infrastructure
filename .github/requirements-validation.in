# Direct CI validation dependencies. The generated .txt file is the install
# authority and includes exact versions plus hashes for every transitive wheel.
ansible-core>=2.20,<2.21
ansible-lint>=26,<27
yamllint>=1.38,<2
