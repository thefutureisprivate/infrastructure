# Non-secret local settings. Provider tokens live in SOPS/infrastructure.sops.yaml.
SSH_PUBLIC_KEY_FILE=/home/you/.ssh/id_ed25519.pub

# Optional image bootstrap settings.
FCOS_STREAM=stable
FCOS_ARCHITECTURE=x86_64
FCOS_LOCATION=fsn1
